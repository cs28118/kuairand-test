Propose one real ranking improvement for KuaiRand validation. Use a complete
unified git diff on an allowed file if you can produce one exactly; otherwise
use an empty git_diff and an existing experiment script. Do not merely invent
a command path, and evaluate only the train and valid development splits.

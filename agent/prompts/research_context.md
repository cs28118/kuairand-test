Run-specific context:

Baseline validation metrics (these are reference values, not a schema):
{baseline_metrics}

Requested research goal:
{goal}

The hypothesis, description, and result comparison must be consistent with this
goal and with the files and APIs listed above. If `git_diff` is empty, describe
an explicit baseline/reproduction measurement rather than claiming a new model
or feature was implemented.
